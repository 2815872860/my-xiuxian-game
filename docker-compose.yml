version: '3.9'
services:
    vue-idle-xiuxian:
        build: .
        image: xiuxian:local
        container_name: xiuxian
        restart: unless-stopped
        ports:
            - 8183:8080
        volumes:
            - ./server/data:/workspace/server/data
        tty: true
        stdin_open: true
